{#
/**
 * @file
 * Template for field_tags.
 */
#}
{% for item in items %}
  {% include '@dorion/badge/badge.twig' with {
    type: 'light',
    content: item.content['#title'],
    url: item.content['#url'],
  } %}
{% endfor %}
